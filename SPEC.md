# SPEC

No feature in flight.

This file is a placeholder. A `SPEC.md` lives at repo root only while a substantive feature is being built (see the `spec` skill). After ship, post-ship cleanup migrates load-bearing invariants into PHPDoc on the enforcing function (or `docs/architecture.md` when conceptual), files open bugs/refactors as GitHub Issues, and truncates this file.

## Last merged

**0.6.0 — Phase 3 handler migration (finish)** (merged to `main`, 2026-07-01; **Unreleased** — no version-header bump, no `v*` tag yet; may combine with later work before release).
The last three legacy handlers (level-restriction, propagation, time-based) migrated `HandlerBase → UnifiedHandlerBase`; all 7 handlers now share the unified base. Legacy `class-handler-base.php` deleted, the redundant `on_post_save` handler loop removed (closed the time-based double-run), scalar `post_type` → plural `post_types` checkboxes, per-handler config label/UX pass, and real Wireframe `conditions` for sibling-conditional subfields. Post-merge review pass fixed the ACF short-circuit + value-independent ACF field discovery (empty-child sync), reentrancy-guard symmetry, the shared post-type extractor, and the bulk `post_types` key.

Invariants migrated to `docs/architecture.md` ("Writing a rule handler — hard-won invariants"): the 3-handler migration lessons are items #10 (parent↔child sync on child `save_post` + `conflict_handling`, was §V12/B3), #11 (a base-class flip must port EVERY base method the handler still calls, was §V14/B4), and #12 (discover ACF fields by location rules not stored values + first-write by key, 0.6.0 ACF sweep). §V1–V6 (typed-helper rebind, `should_process_post` gating, atomic config+handler, loop-removal, hierarchical-only propagation field, time-based single-run) are enforced in the handler/base PHPDoc + CLAUDE.md don't #6; §V7–V11 were already architecture.md items #1–9.

Open bugs / refactors filed as GitHub Issues: [#31](https://github.com/davidofchatham/meta-conductor/issues/31) (bulk process-existing inert for hook-driven handlers), [#32](https://github.com/davidofchatham/meta-conductor/issues/32) (level-restriction keep-ancestors two meanings), [#34](https://github.com/davidofchatham/meta-conductor/issues/34) (propagation inherited-vs-manual tracking), [#35](https://github.com/davidofchatham/meta-conductor/issues/35) (propagation + hierarchical extra expansion level), [#37](https://github.com/davidofchatham/meta-conductor/issues/37) (Admin Columns integration dead on AC v7), [#38](https://github.com/davidofchatham/meta-conductor/issues/38) (ConfigHelpers dedup + mode-switch coupling), [#39](https://github.com/davidofchatham/meta-conductor/issues/39) (cross-handler reentrancy: level-restriction prunes propagated child terms), [#40](https://github.com/davidofchatham/meta-conductor/issues/40) (remove dead handler `validate_rule` overrides), [#41](https://github.com/davidofchatham/meta-conductor/issues/41) (value-independent ACF discovery for level-restriction + related).
